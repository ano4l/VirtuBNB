import { Ionicons } from '@expo/vector-icons';
import * as Linking from 'expo-linking';
import { router } from 'expo-router';
import { useRef } from 'react';
import { ActivityIndicator, KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useAppState } from '@/src/state/AppState';
import { whatsappConfig } from '@/src/config';
import { colors, radius, spacing } from '@/src/theme';
import { useBottomNavClearance } from '@/src/layout';

const prompts = ['What needs attention today?', 'Show open tasks', 'How are my listings performing?', 'Draft a reply to my latest review'];

export default function AssistantScreen() {
  const state = useAppState();
  const scroll = useRef<ScrollView>(null);
  const bottomClearance = useBottomNavClearance();
  const submit = (value = state.assistantDraft) => { void state.sendAssistantMessage(value).then(() => setTimeout(() => scroll.current?.scrollToEnd({ animated: true }), 50)); };
  return <SafeAreaView style={styles.safe}><KeyboardAvoidingView style={styles.safe} behavior={Platform.OS === 'ios' ? 'padding' : undefined} keyboardVerticalOffset={12}>
    <View style={styles.header}><View><Text style={styles.brand}>VirtuHost</Text><View style={styles.status}><View style={styles.dot} /><Text style={styles.statusText}>{state.connected ? 'Workspace connected' : 'Local preview workspace'}</Text></View></View><View style={styles.avatar}><Text style={styles.avatarText}>AY</Text></View></View>
    <ScrollView ref={scroll} style={styles.scroll} contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false}>
      {state.messages.length === 0 ? <View style={styles.welcome}><View style={styles.mark}><Ionicons name="sparkles" size={25} color="#fff" /></View><Text style={styles.kicker}>Your hosting copilot</Text><Text style={styles.title}>What can I take off your plate?</Text><Text style={styles.body}>Ask about today, your listings, guest reviews or work that needs attention.</Text>
        <View style={styles.suggestions}>{prompts.map((prompt) => <Pressable key={prompt} accessibilityRole="button" onPress={() => submit(prompt)} style={({ pressed }) => [styles.suggestion, pressed && styles.pressed]}><Text style={styles.suggestionText}>{prompt}</Text><Ionicons name="arrow-up" size={16} color={colors.green} /></Pressable>)}</View>
        <View style={styles.demoNote}><Ionicons name="information-circle-outline" size={17} color={colors.inkMuted} /><Text style={styles.demoNoteText}>Responses use a local demo assistant with the supported WhatsApp intent vocabulary. No live AI or booking provider is connected.</Text></View>
      </View> : <View style={styles.thread}>{state.messages.map((message) => <View key={message.id} style={[styles.message, message.role === 'user' ? styles.userMessage : styles.assistantMessage]}>{message.role === 'assistant' && <Text style={styles.messageLabel}>VirtuHost</Text>}<Text style={[styles.messageText, message.role === 'user' && styles.userText]}>{message.text}</Text>{message.action && <Pressable accessibilityRole="button" onPress={() => router.push(message.action!.route as never)} style={styles.messageAction}><Text style={styles.messageActionText}>{message.action.label}</Text><Ionicons name="arrow-forward" size={16} color={colors.green} /></Pressable>}</View>)}{state.assistantBusy && <View style={[styles.message, styles.assistantMessage, styles.loading]}><ActivityIndicator color={colors.green} /><Text style={styles.loadingText}>Looking across your workspace</Text></View>}</View>}
    </ScrollView>
    <View style={[styles.composerWrap, { paddingBottom: bottomClearance }]}><View style={styles.composer}><TextInput accessibilityLabel="Ask VirtuHost" placeholder="Ask about your listings..." placeholderTextColor={colors.inkFaint} value={state.assistantDraft} onChangeText={state.setAssistantDraft} onSubmitEditing={() => submit()} returnKeyType="send" style={styles.input} multiline /><Pressable accessibilityRole="button" accessibilityLabel="Send message" disabled={!state.assistantDraft.trim() || state.assistantBusy} onPress={() => submit()} style={({ pressed }) => [styles.send, (!state.assistantDraft.trim() || state.assistantBusy) && styles.sendDisabled, pressed && styles.pressed]}><Ionicons name="arrow-up" size={20} color="#fff" /></Pressable></View>
      <Pressable accessibilityRole="link" onPress={() => Linking.openURL(whatsappConfig.url)} style={styles.whatsapp}><Ionicons name="logo-whatsapp" size={16} color={colors.green} /><Text style={styles.whatsappText}>Continue in WhatsApp</Text></Pressable>
    </View>
  </KeyboardAvoidingView></SafeAreaView>;
}

const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#F7F8F7' }, header: { height: 72, paddingHorizontal: spacing.xl, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', borderBottomWidth: 1, borderBottomColor: '#E5E9E6' }, brand: { color: colors.ink, fontSize: 18, fontWeight: '900', letterSpacing: -0.4 }, status: { flexDirection: 'row', alignItems: 'center', gap: 6, marginTop: 3 }, dot: { width: 7, height: 7, borderRadius: 4, backgroundColor: colors.green }, statusText: { color: colors.inkMuted, fontSize: 12, fontWeight: '700' }, avatar: { width: 42, height: 42, borderRadius: 14, backgroundColor: colors.ink, alignItems: 'center', justifyContent: 'center' }, avatarText: { color: '#fff', fontSize: 12, fontWeight: '900' }, scroll: { flex: 1 }, content: { flexGrow: 1, paddingHorizontal: spacing.xl, paddingBottom: spacing.lg, maxWidth: 640, width: '100%', alignSelf: 'center' }, welcome: { flex: 1, justifyContent: 'center', alignItems: 'center', paddingVertical: 30 }, mark: { width: 58, height: 58, borderRadius: 20, backgroundColor: colors.green, alignItems: 'center', justifyContent: 'center', marginBottom: spacing.xl }, kicker: { color: colors.green, fontSize: 12, fontWeight: '800', letterSpacing: 1.1, textTransform: 'uppercase' }, title: { color: colors.ink, fontSize: 30, lineHeight: 36, fontWeight: '900', textAlign: 'center', letterSpacing: -0.9, maxWidth: 340, marginTop: 9 }, body: { color: colors.inkMuted, fontSize: 14, lineHeight: 21, textAlign: 'center', maxWidth: 330, marginTop: 10 }, suggestions: { width: '100%', gap: 9, marginTop: 26 }, suggestion: { minHeight: 50, borderRadius: radius.md, paddingHorizontal: 15, backgroundColor: '#fff', borderWidth: 1, borderColor: '#E1E6E3', flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, suggestionText: { color: colors.ink, fontSize: 13, fontWeight: '700' }, demoNote: { flexDirection: 'row', gap: 8, marginTop: 18, paddingHorizontal: 6 }, demoNoteText: { flex: 1, color: colors.inkMuted, fontSize: 12, lineHeight: 18 }, thread: { paddingTop: spacing.xl, gap: 14 }, message: { maxWidth: '88%', borderRadius: 18, padding: 15 }, userMessage: { alignSelf: 'flex-end', backgroundColor: colors.ink, borderBottomRightRadius: 5 }, assistantMessage: { alignSelf: 'flex-start', backgroundColor: '#fff', borderWidth: 1, borderColor: '#E1E6E3', borderBottomLeftRadius: 5 }, messageLabel: { color: colors.green, fontSize: 12, fontWeight: '900', marginBottom: 6 }, messageText: { color: colors.ink, fontSize: 14, lineHeight: 21 }, userText: { color: '#fff' }, messageAction: { minHeight: 44, marginTop: 10, paddingTop: 10, borderTopWidth: 1, borderTopColor: colors.line, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, messageActionText: { color: colors.green, fontSize: 12, fontWeight: '800' }, loading: { flexDirection: 'row', alignItems: 'center', gap: 10 }, loadingText: { color: colors.inkMuted, fontSize: 12 }, composerWrap: { paddingHorizontal: spacing.xl, paddingTop: 10, backgroundColor: '#F7F8F7', maxWidth: 640, width: '100%', alignSelf: 'center' }, composer: { minHeight: 58, backgroundColor: '#fff', borderWidth: 1, borderColor: '#DDE3DF', borderRadius: 18, paddingLeft: 16, paddingRight: 7, flexDirection: 'row', alignItems: 'center' }, input: { flex: 1, color: colors.ink, fontSize: 14, maxHeight: 88, paddingVertical: 12 }, send: { width: 44, height: 44, borderRadius: 14, backgroundColor: colors.green, alignItems: 'center', justifyContent: 'center' }, sendDisabled: { backgroundColor: '#A9B4AE' }, whatsapp: { minHeight: 44, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 7 }, whatsappText: { color: colors.green, fontSize: 12, fontWeight: '800' }, pressed: { opacity: 0.75, transform: [{ scale: 0.98 }] } });
